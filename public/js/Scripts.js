/**
 * Configuración de rutas o valores cambiantes.
 */

var RutaImagen = '../../../resources/images/';
var RutaDocumento = '../../../resources/documents/';

/**
 * Inicializador de Plugins.
 */

$(document).ready(function () {
    $('.form-image').fileinput({'showUpload': false});
    $('.text-editor').Editor();
});


